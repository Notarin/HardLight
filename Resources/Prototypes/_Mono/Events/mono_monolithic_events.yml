# VRS: Ported from Triad_Sector — entity table selector for Monolithic fragment events.

- type: entityTable
  id: MonoMonolithicEventsTable
  table: !type:AllSelector
    children: # we need to pass a list of rules, since rules have further restrictions to consider via StationEventComp
    - id: MonolithFragmentSmallAppearance
